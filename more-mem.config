process {
    errorStrategy = { task.exitStatus in [104,134,135,137,139,140,143,247] ? 'retry' : 'terminate' }

    withName: RECODING_LANDSCAPE {
        maxRetries = 2
        memory     = { 12.GB * (2 ** (task.attempt - 1)) }   // 12 / 24 / 48 GB
    }
}
